import {
  useLocation,
  Outlet,
  NavLink,
  useSearchParams,
} from "react-router-dom";
import { testData } from "./Message";

const Messages = () => {
  return (
    <div>
      <p>Messages</p>
    </div>
  );
};

export default Messages;
